# Nuvra CRM - Environment Configuration Template
# Copy this to .env and configure your values

# Application
NEXT_PUBLIC_APP_NAME=Nuvra CRM

# API Configuration (leave empty for mock/demo mode)
NEXT_PUBLIC_API_BASE=

# Development Bootstrap
# Set to 'true' to enable mock data and demo mode (when Supabase not configured)
NEXT_PUBLIC_DEV_BOOTSTRAP=false

# Supabase Configuration (REQUIRED for production)
# Get these from your Supabase project settings at https://supabase.com/dashboard
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (REQUIRED for admin operations and scripts)
# KEEP THIS SECRET - never expose in client-side code
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Development Service Key (REQUIRED for seed/export endpoints)
# Used to protect /api/dev/* endpoints
# Generate a secure random string for production
SERVICE_KEY=your-secure-dev-service-key-change-this

# n8n Webhook Configuration (OPTIONAL)
# If provided, new leads will trigger this webhook for automation
# Leave empty to disable webhook integration
N8N_WEBHOOK_URL=

# Meta Ads Integration (OPTIONAL)
# Verification token for Meta webhook validation
# Set this to match what you configure in Meta Business Manager
META_VERIFY_TOKEN=nuvra_crm_verify

# External Integrations
# Add any custom integration URLs or API keys below
# CUSTOM_API_KEY=
# EXTERNAL_WEBHOOK_URL=
